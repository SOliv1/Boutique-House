import os

# Copy this file to env.py and add your private local credentials.
# env.py is ignored by Git and must never be committed.

os.environ['DEVELOPMENT'] = '1'
# Leave disabled while the site is a prototype. Set to '1' at public launch.
os.environ['SEO_INDEXING_ENABLED'] = '0'
os.environ['PUBLIC_SITE_URL'] = (
    'https://boutique-house-production-751b.up.railway.app'
)
os.environ['STRIPE_PUBLIC_KEY'] = 'pk_test_replace_me'
os.environ['STRIPE_SECRET_KEY'] = 'sk_test_replace_me'

# Preferred for deployed transactional email (HTTPS API).
os.environ['RESEND_API_KEY'] = 're_replace_me'
os.environ['DEFAULT_FROM_EMAIL'] = (
    'Boutique House <onboarding@resend.dev>'
)

# Optional local SMTP alternative.
os.environ['SEND_REAL_EMAILS'] = '1'
os.environ['EMAIL_HOST'] = 'smtp.mail.me.com'
os.environ['EMAIL_PORT'] = '587'
os.environ['EMAIL_HOST_USER'] = 'your-address@icloud.com'
os.environ['EMAIL_HOST_PASS'] = 'your-apple-app-specific-password'
os.environ['DEFAULT_FROM_EMAIL'] = 'your-address@icloud.com'
